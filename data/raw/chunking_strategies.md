Fixed-size chunking divides documents into uniform segments of N tokens with a configurable overlap window. Recursive character splitting uses structural delimiters like paragraph breaks and newlines to produce semantically coherent fragments. Semantic chunking embeds adjacent sentences and splits where the cosine distance between consecutive sentence vectors exceeds a percentile threshold. Chunk size directly impacts retrieval granularity: smaller chunks improve precision but may lose surrounding context.