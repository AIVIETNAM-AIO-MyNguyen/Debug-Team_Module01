Parent-Document retrieval indexes small child chunks to maintain precise lexical or semantic alignment with short search queries. However, during synthesis, the retriever maps the matched child ID to its larger parent document segment. This ensures the generative LLM receives sufficient surrounding context padding to ground its answer accurately. Contextual compression uses similarity filters to prune noisy sentences from the retrieved text, but is redundant when parent padding is desired.